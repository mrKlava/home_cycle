const ERRORS = {
  'MISSING_REQUIRED_FIELDS': 'All required fields must have value',
  'PASSWORD_SHORT': 'Password must be at least 8 characters',
  'PASSWORD_NOT_MATCH': 'Password do not match',
};

Object.freeze(ERRORS);

export default ERRORS;